# Sample EncoderDecoder configuration with ResNeXt blocks
block_factory = "InvertedBottleneck"
in_channels = 3  # Will be overridden by dataset features
channels = [36, 64, 96, 192, 256, 384]
depths = [1, 2, 2, 3, 3, 4]
out_channels = 1
bilinear = true  # Use transpose convolution for upsampling
